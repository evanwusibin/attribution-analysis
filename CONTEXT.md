# 领域上下文

## 证据语义

| 标记 | 含义 | 结论约束 |
|---|---|---|
| `FACT` | 可定位的手册、规范或 DMS 需求事实 | 仅在事实覆盖范围内表达 |
| `MOCK` | 固定演示数据或待确认阈值 | 界面必须显示模拟性质 |
| `MISSING` | 未获得或未证实的关键资料 | 输出补数清单与人工复核 |

## 核心对象

- `AttributionCase`：一次归因请求及其生命周期。
- `AnalysisPlan`：待执行的证据获取计划。
- `ToolExecution`：一次工具调用的输入、结果、错误和时长。
- `Evidence`：带来源、等级和版本的证据。
- `AttributionResult`：问题、指标、证据、结论、缺失数据和下一步。

## 禁止含义

“建议”不是自动处置；“候选根因”不是最终归责；“模拟规则命中”不是业务制度命中。
