# 01 Brief

## 目标

建立可重复运行的独立工程基线，为后续证据驱动的经营归因切片提供承载边界。

## 范围

- FastAPI 服务与 `GET /health`。
- 可编辑安装、自动化测试、环境样例、忽略规则和协作上下文。
- 证据等级约束和独立工程边界。

## 非范围

归因编排、数据库、LLM、RAG、NL2SQL、业务规则、真实 BYD 或瑞能数据连接。

## 成功标准

在干净虚拟环境中，安装开发依赖后 `pytest` 通过，且 `GET /health` 返回稳定的服务状态。
