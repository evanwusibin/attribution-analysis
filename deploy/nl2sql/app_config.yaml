logging:
  file: {enable: true, level: INFO, path: logs, rotation: 10 MB, retention: 7 days}
  console: {enable: true, level: INFO}
db_meta:
  host: mysql
  port: 3306
  user: attribution
  password: ${MYSQL_PASSWORD}
  database: attribution_business
db_dw:
  host: mysql
  port: 3306
  user: attribution
  password: ${MYSQL_PASSWORD}
  database: attribution_business
qdrant: {host: qdrant, port: 6333, embedding_size: 1024}
embedding: {host: embedding, port: 80, model: ${NL2SQL_EMBEDDING_MODEL_ID}}
es: {host: elasticsearch, port: 9200, index_name: data-agent-column}
llm:
  model_name: ${NL2SQL_LLM_MODEL}
  api_key: ${NL2SQL_LLM_API_KEY}
  base_url: ${NL2SQL_LLM_BASE_URL}
