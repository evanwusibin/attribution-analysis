# 经营归因分析系统 · 全链路项目复习

> **一句话定位**：基于 LangGraph Agent + 13 个业务工具的经营归因系统，输出六段式结论 + `FACT`/`MOCK`/`MISSING` 三色证据 + 人工复核边界。
> **技术栈**：FastAPI + SQLAlchemy + DuckDB/PostgreSQL + LangGraph + Milvus + RapidOCR
> **仓库**：`evanwusibin/attribution-analysis`

---

## 一、项目定位与核心价值

传统经营归因依赖"老师傅凭经验判根因"，信息碎片化、无法追溯、缺乏审计。本系统用 LangGraph Agent + 13 个工具（9 售后 + 4 售前），把异常组织为"输入 → 证据 → 受控结论"的可追溯链路。

**面试话术**：
> "归因系统的核心是三色证据体系——`FACT` 已验证事实、`MOCK` 模拟推演、`MISSING` 缺失待补。关键结论缺失时不自动裁决、不扣款，始终以人工复核为最终边界。这样保证了审慎性。"

---

## 二、系统架构（六层）

```
┌─────────────────────────────────────────┐
│           前端（原生 HTML/CSS/JS）         │
│  轮播登录 → 归因工作台 → 证据中心/会话历史 │
│  1.mp4 视频 + 白字首屏轮播               │
├─────────────────────────────────────────┤
│           API 层（FastAPI）               │
│  Auth（cookie 会话）                     │
│  Cases / Tools / After-sales / RAG       │
├─────────────────────────────────────────┤
│           领域层                          │
│  Case → Plan → ToolExecution → Evidence  │
│  三色证据不变量（FACT/MOCK/MISSING）      │
│  状态机：pending → running → done/ blocked│
├─────────────────────────────────────────┤
│           应用层（13 工具）               │
│  售后 9：保内外/原厂件/电池/供应商/星级等  │
│  售前 4：漏斗/流失/报价/线索              │
│  公共内核：NL2SQL + RAG 双路证据          │
├─────────────────────────────────────────┤
│           引擎层                          │
│  LangGraph 编排（query plan + tools）     │
│  RRF 融合 / Rerank / 置信度判断           │
├─────────────────────────────────────────┤
│           基础设施层                      │
│  DuckDB（演示）/ PostgreSQL + pgvector    │
│  MySQL（认证独立库）                      │
│  Milvus（向量检索）+ Qdrant/ES           │
│  MinIO（附件）+ Embedding（BGE-M3）       │
└─────────────────────────────────────────┘
```

---

## 三、核心实体与状态机

### 领域模型

| 实体 | 说明 |
|------|------|
| Case（案件） | 一个待归因的经营异常，如"电池包 SOC 异常" |
| Plan（计划） | 分析路径规划，决定调哪些工具 |
| ToolExecution（工具执行） | 调用 13 个工具之一，返回结构化证据 |
| Evidence（证据） | 三色分级：FACT（已验证）、MOCK（模拟）、MISSING（缺失） |
| Result（结论） | 六段式输出：问题摘要/根因链/证据清单/修复建议/人工复核项/风险提示 |

### 状态机

```
pending → running → done
                ├→ blocked（证据不足需人工）
                └→ failed（工具调用失败）
```

### 三色证据不变量（强制）

- `MISSING` 或仅 `MOCK` 的关键结论 → 只能要求人工复核，**不能自动审批/扣款/降级/追偿**
- 证据必须标识 `FACT`/`MOCK`/`MISSING`，并带来源定位
- 工具层默认只读；外部能力必须通过适配器接入

---

## 四、13 个业务工具

### 售后 9 个

| 工具 | 功能 | 证据类型 |
|------|------|---------|
| 保内外判断 | 根据 VIN + 日期判断是否在质保期 | FACT |
| 原厂件检查 | 检查配件是否为原厂件 | FACT |
| 保养记录查询 | 查 VIN 对应的保养记录 | FACT |
| 电池健康度 | SOH/SOC/循环次数 | FACT |
| 供应商不良率 | 供应商质量问题统计 | FACT |
| 星级评分引擎 | 客户满意度评分 | MOCK |
| 工单查询 | 查历史工单 | FACT |
| NL2SQL 问数 | 自然语言转 SQL 查业务数据 | FACT |
| RAG 知识检索 | 向量检索政策/手册 | FACT/MOCK |

### 售前 4 个

| 工具 | 功能 |
|------|------|
| 漏斗转化分析 | 商机转化率分析 |
| 流失评分 | 客户流失风险评估 |
| 报价偏差分析 | 报价竞争力对比 |
| 线索来源分析 | 线索质量归因 |

---

## 五、Docker 部署

### 一键启动（`compose.yaml`）

```bash
cd attribution-analysis
# 需宿主 MySQL:3307 / Qdrant:6333 / ES:9200 / Embedding:8081 已运行
docker compose up -d
# 前端 http://127.0.0.1:5173  后端 http://127.0.0.1:8002
```

**服务编排**（9 组件）：
- 归因后端（`8002`）+ 前端（`5173`）
- PostgreSQL + pgvector、MySQL、MongoDB、MinIO
- Milvus、Qdrant、Elasticsearch、Embedding

### 演示路径

```bash
# 登录
curl -X POST http://127.0.0.1:8002/api/v1/auth/login \
  -H "Content-Type: application/json" -d '{"username":"analyst","password":"analyst123"}' -c /tmp/cookies.txt

# 电池包诊断
curl -b /tmp/cookies.txt -X POST http://127.0.0.1:8002/api/v1/after-sales/diagnostics \
  -H "Content-Type: application/json" -d '{"question":"电池包SOC异常，SOH只有70%","vin":"LSGAB52R7DF000005"}'

# 索赔合规
curl -b /tmp/cookies.txt -X POST http://127.0.0.1:8002/api/claim-compliance/analyze \
  -H "Content-Type: application/json" -d '{"claim_id":"CLM-2026-001"}'
```

---

## 六、前端关键点

### 登录轮播（`public/images/`）

- 左侧：`1.mp4` 视频（5s）→ `2.webp/3-4.png` 图片自动循环
- 首屏文案：`42px` 白字居中左对齐（"可追溯的经营分析协作空间"）
- 左上常驻品牌：`归因分析平台 / ATTRIBUTION INTELLIGENCE`（白字，无深蓝徽标）
- 右侧：登录表单（`analyst/analyst123` 或 `admin/admin123`）

### 工作台

- 侧边栏：归因工作台/会话历史/证据中心/分析轨迹/导出中心
- 主内容：Case 列表 + 工作流时间线（实时打字机效果）
- 证据面板：三色渲染（FACT 绿/橙/MISSING 红）

---

## 七、六段式输出结构

```json
{
  "问题摘要": "电池包 SOC 异常，SOH 仅 70%，低于同批次均值 15%",
  "根因链": "高温运营 → 电芯内阻上升 → 容量衰减 → SOH 下降",
  "证据清单": [
    {"type": "FACT", "source": "电池健康度工具", "content": "SOH 70%, 循环 892 次"},
    {"type": "MISSING", "source": "保养记录", "content": "未找到该 VIN 保养记录"}
  ],
  "修复建议": "建议更换电池模组，同步排查热管理系统",
  "人工复核项": ["保养记录缺失需人工补录", "供应商责任待确认"],
  "风险提示": "MISSING 保养记录，不得自动追偿供应商"
}
```

---

## 八、已知限制

1. **旧项目依赖**：`adapters/` 参考旧项目 `query_server.py` / `main.py`，不得向领域层扩散
2. **MOCK 数据**：模拟库仅用于演示，不得表述为生产接入
3. **NL2SQL 安全**：不接受任意 SQL，只走白名单查询
4. **Milvus 端口**：`docker-compose.milvus.yml` 中 MinIO 端口改为 `19000`（避免与合同评审 MinIO 冲突）

---

## 九、面试高频问题

**Q：三色证据体系的含义？**
A：`FACT` 已验证事实、`MOCK` 模拟推演、`MISSING` 缺失待补。关键结论仅依赖 `MOCK` 或 `MISSING` 时只能要求人工复核。

**Q：与 RAG 的关系？**
A：RAG（Milvus 向量检索 + BM25）负责事实召回，归因系统负责证据组织和结论生成。两者通过 `ports/` 契约解耦。

**Q：13 个工具怎么扩展？**
A：在 `adapters/` 增加实现并保持 `ports/` 契约，不复制旧项目代码。

**Q：LangGraph 的优势？**
A：每个阶段拆成独立节点，便于调试、观测和扩展。主体确认失败可提前返回候选，闲聊可跳过检索，非库内信息可走 MCP 搜索。

---

## 十、项目边界

- 教学/实战项目，不含完整企业权限体系、灰度发布和多租户治理
- 模拟数据和 `MOCK` 证据只服务可复现实战演示
- RAG 回答质量依赖知识库质量、切片策略和 LLM 稳定性
- 微调模型（`crm-finetune`）不能替代 RAG 的事实检索，二者互补

---

> **复习时间**：30 分钟通读 + 15 分钟 API 试跑
> **快速启动**：`docker compose up -d` → `analyst/analyst123`
